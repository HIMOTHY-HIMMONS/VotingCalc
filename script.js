function cal() {
    var input = document.getElementById("input").value;

    if (input >= 18) {
        alert("You are allowed to vote")
    }

    else {
        alert("You still have to wait!")
    }
}